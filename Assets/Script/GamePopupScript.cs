using UnityEngine;
using UnityEngine.UI;

namespace Script
{
    public class GamePopupScript : MonoBehaviour
    {
        [SerializeField] private GameObject _panel;
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
